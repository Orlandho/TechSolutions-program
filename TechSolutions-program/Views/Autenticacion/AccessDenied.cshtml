@{
    ViewData["Title"] = "Acceso Denegado";
}

<div class="container">
    <div class="row justify-content-center mt-5">
        <div class="col-md-8 col-lg-6">
            <div class="card border-danger shadow">
                <div class="card-header bg-danger text-white text-center">
                    <h3 class="mb-0">
                        <i class="bi bi-shield-x"></i> Acceso Denegado
                    </h3>
                </div>
                <div class="card-body text-center p-4">
                    <div class="mb-4">
                        <i class="bi bi-exclamation-triangle text-danger" style="font-size: 4rem;"></i>
                    </div>
                    
                    <h4 class="card-title text-danger">No tienes permisos suficientes</h4>
                    
                    <p class="card-text text-muted mt-3">
                        Lo sentimos, pero no tienes los permisos necesarios para acceder a esta funcionalidad.
                    </p>

                    <div class="alert alert-warning mt-4" role="alert">
                        <i class="bi bi-info-circle-fill"></i>
                        <strong>¿Necesitas acceso?</strong><br/>
                        Contacta al administrador del sistema o al líder de proyecto para solicitar los permisos adecuados.
                    </div>

                    <div class="mt-4">
                        <p class="text-muted small">
                            <strong>Tu rol actual:</strong> 
                            @if (User.IsInRole("Administrador"))
                            {
                                <span class="badge bg-danger">Administrador</span>
                            }
                            else if (User.IsInRole("Lider"))
                            {
                                <span class="badge bg-primary">Líder</span>
                            }
                            else if (User.IsInRole("Desarrollador"))
                            {
                                <span class="badge bg-success">Desarrollador</span>
                            }
                            else
                            {
                                <span class="badge bg-secondary">Sin rol asignado</span>
                            }
                        </p>
                    </div>

                    <div class="mt-4">
                        <a asp-controller="Seguimiento" asp-action="Index" class="btn btn-primary">
                            <i class="bi bi-house-door"></i> Volver al Dashboard
                        </a>
                        <button onclick="history.back()" class="btn btn-outline-secondary">
                            <i class="bi bi-arrow-left"></i> Volver Atrás
                        </button>
                    </div>
                </div>
                <div class="card-footer text-muted text-center small">
                    Si crees que esto es un error, contacta a soporte técnico.
                </div>
            </div>
        </div>
    </div>
</div>
