@model IEnumerable<TechSolutions_program.Models.Tarea>

@{
    ViewData["Title"] = "Mis tareas";
}

<h1 class="h3">Mis tareas</h1>

@if (!Model.Any())
{
    <div class="alert alert-info mt-3">No tienes tareas asignadas.</div>
}
else
{
    <div class="table-responsive mt-3">
        <table class="table table-striped align-middle">
            <thead>
                <tr>
                    <th>Descripción</th>
                    <th>Estado</th>
                    <th>Prioridad</th>
                    <th>Fecha límite</th>
                    <th>Proyecto</th>
                    <th class="text-end">Acciones</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var tarea in Model)
                {
                    <tr>
                        <td>@tarea.Descripcion</td>
                        <td>
                            <span class="badge @(tarea.Estado == "Finalizado" ? "bg-success" : tarea.Estado == "En Progreso" ? "bg-warning text-dark" : "bg-secondary")">
                                @tarea.Estado
                            </span>
                        </td>
                        <td>
                            <span class="badge @(tarea.Prioridad == "Alta" ? "bg-danger" : tarea.Prioridad == "Media" ? "bg-warning text-dark" : "bg-info")">
                                @tarea.Prioridad
                            </span>
                        </td>
                        <td>@(tarea.FechaLimite.HasValue ? tarea.FechaLimite.Value.ToString("dd/MM/yyyy") : "-")</td>
                        <td>@tarea.ProyectoId</td>
                        <td class="text-end">
                            <form asp-action="CambiarEstado" asp-route-id="@tarea.Id" method="post" class="d-inline">
                                @Html.AntiForgeryToken()
                                @if (tarea.Estado == "Pendiente")
                                {
                                    <button type="submit" name="nuevoEstado" value="En Progreso" class="btn btn-sm btn-outline-warning">Iniciar</button>
                                }
                                else if (tarea.Estado == "En Progreso")
                                {
                                    <button type="submit" name="nuevoEstado" value="Finalizado" class="btn btn-sm btn-outline-success">Completar</button>
                                }
                                else
                                {
                                    <span class="text-success">Completada</span>
                                }
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
}

@section Scripts {
    @* Sección para scripts específicos de esta vista *@
    <script>
        // Aquí puedes agregar scripts JavaScript específicos para la vista MisTareas
    </script>
}

